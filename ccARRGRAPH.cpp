#include<bits/stdc++.h>
using namespace std;

void test() {
    scanf("%d", &n);
    for (int i = 1; i <= n; ++i) scanf("%d", &n);
}

int main() {
    int t;
    scanf("%d", &t);
    while (t--) {
        test();
    }
}