#include<bits/stdc++.h>
using namespace std;


struct person
{
    int b,d,f;
    bool operator < (person& other) {
        return 
    }
};


int main() {
    scanf("%d", &n);
    for (int i = 0; i < n; ++i) {
        scanf("%d%d%d", %b, %d, %f);

    }
    
}